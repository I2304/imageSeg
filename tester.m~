clear all; close all; clc; 
path = './sample_images/usnccm/original.png'; % relative path to the image
p = 1.5;q = 2;r = .5;                         % p, q, r
lim = 60;                                     % limit (max eigemvalue
k = 6;K=7;

filename='usnccm';reps=5;

swap = true; 
addpath('./testing')
[spec, truth, K] = test_img(3, true, .25);
[TA, CA] = run_segmentation('kmeans', path, swap, p, q, r, lim, k, K, ...
    'testingtesting');